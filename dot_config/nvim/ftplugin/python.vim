set foldmethod=indent  " 'syntax' does not work for some reason with Python,
                       " but 'indent' is enough for this language

" For highlighted trailing whitespace and mix of spaces and tabs
let python_space_error_highlight=1
